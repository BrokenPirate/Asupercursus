<h1 class="text-center"> Détails du cours </h1>

<h2 class="text-center">
	<%= @course.name %>
</h2>

<h3 class="text-center">
	url: <%= @course.url %>
</h3>

<p class="text-center">


	<%= link_to "Edit course", edit_course_path(@course), class: "btn btn-warning" %>
	<%= link_to "Delete course", course_path(@course), method: :delete, class: "btn btn-danger" %>
	<%= link_to "Back to courses list", courses_path, class: "btn btn-primary" %>

</p>

